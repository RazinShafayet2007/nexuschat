{
  "name": "nexuschat-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "client": "npm run dev --workspace=packages/client",
    "server": "npm run dev --workspace=packages/server"
  }
}